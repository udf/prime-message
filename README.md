Simple algorithm for encoding an ASCII string of text as a large composite number where each prime factor represents a character in the message

usage:  
`encode.py string of text`  
`decode.py n [n] [...]`